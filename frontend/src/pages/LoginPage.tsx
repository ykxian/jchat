export function LoginPage() {
  return (
    <section className="page">
      <p className="eyebrow">Auth</p>
      <h2>Login</h2>
      <p>Phase 0 只提供页面占位，后续在 Phase 4 接入真实登录流程。</p>
    </section>
  );
}

